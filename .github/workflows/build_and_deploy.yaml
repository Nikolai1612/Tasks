name: Build and Deploy ASP.NET Core App

on:
  push:
    branches:
      - test

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: '8.x'

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration Release --no-restore

    - name: Publish
      run: dotnet publish --configuration Release --no-restore

    - name: Create web.config
      run: |
        echo "<?xml version=\"1.0\" encoding=\"utf-8\"?>" > web.config
        echo "<configuration>" >> web.config
        echo "  <location path=\".\" inheritInChildApplications=\"false\">" >> web.config
        echo "    <system.webServer>" >> web.config
        echo "      <handlers>" >> web.config
        echo "        <add name=\"aspNetCore\" path=\"*\" verb=\"*\" modules=\"AspNetCoreModuleV2\" resourceType=\"Unspecified\" />" >> web.config
        echo "      </handlers>" >> web.config
        echo "      <aspNetCore processPath=\"dotnet\" arguments=\".\Tasks.dll\" stdoutLogEnabled=\"false\" stdoutLogFile=\".\logs\stdout\" hostingModel=\"inprocess\">" >> web.config
        echo "        <environmentVariables>" >> web.config
        echo "          <environmentVariable name=\"MySetting\" value=\"MyValue\" />" >> web.config
        echo "          <environmentVariable name=\"AnotherSetting\" value=\"AnotherValue\" />" >> web.config
        echo "        </environmentVariables>" >> web.config
        echo "      </aspNetCore>" >> web.config
        echo "    </system.webServer>" >> web.config
        echo "  </location>" >> web.config
        echo "</configuration>" >> web.config
        echo "<!--ProjectGuid: BB1997CA-7460-4A16-85A8-879E521560F7-->" >> web.config

    - name: Run tests
      run: dotnet test --no-restore --verbosity normal

    - name: Upload build artifacts
      uses: actions/upload-artifact@v2
      with:
        name: web-app
        path: |
          web.config

    - name: Deploy to somee.com via FTP
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        protocol: ftp
        local-dir: /
        server-dir: /www.tasks.somee.com/
